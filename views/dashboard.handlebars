{{>header}}
<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Email Sent</title>
  <link rel="stylesheet" href="" />
  <style>

  </style>
</head>

<body>
  <div class="container-fluid">
    <div class="row">
      <div class="col-md-3 side_bar ">
        <div class="d-flex flex-column flex-shrink-0 p-3 bg-light" style="width: 280px;">
          <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-dark text-decoration-none">
            <span class="fs-4">Sidebar</span>
          </a>
          <hr>
          <ul class="nav nav-pills flex-column mb-auto">
            <li class="nav-item">
              <a href="/add_new" class="nav-link link-dark" aria-current="page">
                Add New Hotel Details
              </a>
            </li>
            <li>
              <a href="/updatePage" class="nav-link link-dark">
               Update Hotel Details
              </a>
            </li>
            <li>
          </ul>
          <hr>
          <form action="/logout" method="POST">
            <button onclick="deleteLocal()">Log Out</button>
          </form>
        </div>
      </div>
      <div id="username" class="col-md-8 text-center">

      </div>

      <div class="col-md-1"></div>
    </div>
  </div>
</body>
<script>
  const user = JSON.parse((localStorage.getItem("currentLoggedIn")));
  const username = document.createElement("h2");
  username.innerHTML = 'Welcome ' + user.name + ' ' + user.surname + ' ' + `(${user.role})`;
  document.getElementById("username").appendChild(username);

  function deleteLocal(){
    window.localStorage.clear();
  }


</script>

</html>
{{>footer}}